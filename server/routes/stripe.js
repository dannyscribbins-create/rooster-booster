// Stripe ACH payout routes — placeholder
// Will implement payout infrastructure when Stripe Connect is built
const express = require('express');
const router = express.Router();
module.exports = router;
